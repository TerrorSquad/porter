{
  "name": "utils",
  "version": "1.0.0",
  "description": "Collection of JS/TS utilities",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "porter": "tsx src/porter.ts",
    "build:rollup": "rollup -c",
    "build": "pnpm run build:external",
    "build:external": "EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.mjs pnpm run build:rollup && chmod +x dist/porter.mjs && pnpm run size:report",
    "build:no-base64": "PORTER_FEATURE_BASE64=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.no-base64.mjs pnpm run build:rollup && chmod +x dist/porter.no-base64.mjs && pnpm run size:report",
    "build:single-file-only": "PORTER_FEATURE_MULTI_PART_INPUT=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.single-file.mjs pnpm run build:rollup && chmod +x dist/porter.single-file.mjs && pnpm run size:report",
    "build:no-invert": "PORTER_FEATURE_INVERT=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.no-invert.mjs pnpm run build:rollup && chmod +x dist/porter.no-invert.mjs && pnpm run size:report",
    "build:no-multi-qr": "PORTER_FEATURE_MULTI_QR=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.no-multi-qr.mjs pnpm run build:rollup && chmod +x dist/porter.no-multi-qr.mjs && pnpm run size:report",
    "build:slideshow-only": "PORTER_FEATURE_INTERACTIVE_CONTROLS=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.slideshow-only.mjs pnpm run build:rollup && chmod +x dist/porter.slideshow-only.mjs && pnpm run size:report",
    "build:slim": "pnpm run build:slideshow-only",
    "build:minimal": "PORTER_FEATURE_BASE64=false PORTER_FEATURE_MULTI_PART_INPUT=false PORTER_FEATURE_INVERT=false PORTER_FEATURE_MULTI_QR=false PORTER_FEATURE_INTERACTIVE_CONTROLS=false EXTERNAL_DEPS=qrcode-generator OUTPUT_FILE=dist/porter.minimal.mjs pnpm run build:rollup && chmod +x dist/porter.minimal.mjs && pnpm run size:report",
    "build:standalone": "OUTPUT_FILE=dist/porter.standalone.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.mjs && pnpm run size:report",
    "build:standalone:no-base64": "PORTER_FEATURE_BASE64=false OUTPUT_FILE=dist/porter.standalone.no-base64.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.no-base64.mjs && pnpm run size:report",
    "build:standalone:single-file-only": "PORTER_FEATURE_MULTI_PART_INPUT=false OUTPUT_FILE=dist/porter.standalone.single-file.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.single-file.mjs && pnpm run size:report",
    "build:standalone:no-invert": "PORTER_FEATURE_INVERT=false OUTPUT_FILE=dist/porter.standalone.no-invert.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.no-invert.mjs && pnpm run size:report",
    "build:standalone:no-multi-qr": "PORTER_FEATURE_MULTI_QR=false OUTPUT_FILE=dist/porter.standalone.no-multi-qr.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.no-multi-qr.mjs && pnpm run size:report",
    "build:standalone:slideshow-only": "PORTER_FEATURE_INTERACTIVE_CONTROLS=false OUTPUT_FILE=dist/porter.standalone.slideshow-only.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.slideshow-only.mjs && pnpm run size:report",
    "build:standalone:slim": "pnpm run build:standalone:slideshow-only",
    "build:standalone:minimal": "PORTER_FEATURE_BASE64=false PORTER_FEATURE_MULTI_PART_INPUT=false PORTER_FEATURE_INVERT=false PORTER_FEATURE_MULTI_QR=false PORTER_FEATURE_INTERACTIVE_CONTROLS=false OUTPUT_FILE=dist/porter.standalone.minimal.mjs pnpm run build:rollup && chmod +x dist/porter.standalone.minimal.mjs && pnpm run size:report",
    "build:bundle": "pnpm run build:standalone",
    "install:local": "pnpm run build:standalone && cp dist/porter.standalone.mjs ~/.local/bin/porter && chmod +x ~/.local/bin/porter",
    "size:report": "node scripts/report-sizes.mjs",
    "test": "tsx src/lib/chunker.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.1",
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.9",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-replace": "^6.0.3",
    "@rollup/plugin-terser": "^1.0.0",
    "@rollup/plugin-typescript": "^12.3.0",
    "@types/node": "^20.11.24",
    "rollup": "^4.59.0",
    "tslib": "^2.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
