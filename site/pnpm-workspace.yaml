# Own workspace root, so pnpm never walks up and node_modules can't appear at
# the repo root. The repo above has no JavaScript in it (ADR-0006).
allowBuilds:
  '@parcel/watcher': true
  better-sqlite3: true
  esbuild: true
  sharp: true
  unrs-resolver: true
  vue-demi: true
