---
title: Receiving a file
description: Scanning with the app, resuming an interrupted transfer, and the two paths that don't need a camera.
navigation:
  icon: i-lucide-scan-line
---

Open the receiver, point it at the terminal. Scanning starts automatically —
there is no button to press and no session to establish, because the sender does
not know you exist.

## What happens as you scan

Each decoded QR payload is parsed, handed to a worker isolate, and written to
disk as it resolves. The UI shows live progress; the decoding never runs on the
UI thread, which is what keeps a 26,000-chunk transfer responsive.

On completion the app verifies SHA-256 against the `CHECKSUM` frame and saves to
Downloads.

::warning
Fountain progress is not linear against `K`. Peeling needs materially more than
`K` distinct symbols — measured between 1.33× and 1.89× across real transfers —
so a bar scaled to `K` sits at 99% with a third of the scanning left. The app
scales to ~2×`K` for this reason.
::

## Resuming

Chunks are written to disk as they decode, one file per recovered block. If the
app is killed or evicted, it rehydrates from those files on the next launch —
no re-scanning what you already caught.

Hydration trusts the **chunk filenames**, not the metadata file. Metadata writes
are debounced to at most one every five seconds, so a crash can leave them
behind what's actually on disk; a `chunk_NNNNNN.bin` that exists was fully
written.

## Without a camera

Two subcommands finish a transfer when the bytes are already somewhere useful.

### `serve` — over a network

```bash
porter-sender serve --port=8080 --output-dir=received
```

An HTTP receiver on `axum`. `POST /upload` accepts raw bodies,
`multipart/form-data`, and QR-scan JSON — sequential frames, fountain frames and
checksum frames alike — and auto-joins completed transfers.

Useful when the air gap turns out to be one-way: scan what you must, then finish
over a wire once the file is somewhere with a network.

### `join` — from parts on disk

```bash
porter-sender join <transfer-dir|file|id> [--output <path>] [--force] [--no-verify]
```

Reassembles the `.partaa`, `.partab`, … files a receiver wrote into the original
file, verifying against `<base>.sha256` when one is present. The target can be a
transfer directory, any file inside one, or a bare transfer id relative to the
working directory.

Output goes to `<base>.joined` next to the parts unless `--output` says
otherwise. An existing destination is never overwritten without `--force`.

::note
Stray files that merely share a `.part` prefix are skipped rather than
concatenated in — a deliberate difference from the TypeScript implementation
this was ported from.
::
